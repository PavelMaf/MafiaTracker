# AGENTS Instructions

- Всегда запускай `npm run test` и `npm run build` перед завершением.
- Не добавляй тяжёлые зависимости без необходимости.
- Не делай сервер, всё локально.
- Данные правил и ролей хранить в `src/domain/roles.ts` и `src/domain/settings.ts`.
